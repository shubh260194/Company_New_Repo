package Collection;

// import java.util.Collection;
// represent grp of individual objects as single entity

public class Collection {
	
	
	// Interface : contains most common methods 
	// which are applicable for any collection objects
	// Object > Iterable > Collection 
	// there is no concrete class, which implements collection interface directly
	// collection : interface 
	// collections : utility class , defines several utility methods for collection objects 
	// ex. collectons.sort(AL) , sorting, searching etc.

	public static void main(String[] args) {
		// boolean add (object o)
		// boolean addAll (collection c) : collection of objects
		// boolean remove (object o)
		// boolean removeAll (collection c) : collection of objects
		// void clear () remove all objects
		// retainAll (collection c) : except collection c all will be removed
        // boolean isEmpty () :check collection is empty
		// int size () : give size of collection
		// boolean contains (object o) : search
		// boolean containsAll (collections c) : search
		// object[] a = c.toArray () : return type - object >> Array object[] toArray
		// Iterator iterator () ;
		
	}

}
